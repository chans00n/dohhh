{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd dohhh && yarn install && NODE_ENV=production node run-migrations.js"
  },
  "deploy": {
    "startCommand": "cd dohhh && NODE_ENV=production DISABLE_ADMIN=true node railway-debug.js && NODE_ENV=production DISABLE_ADMIN=true node start-api-only.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}