{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd dohhh && yarn install && yarn build"
  },
  "deploy": {
    "startCommand": "cd dohhh && NODE_ENV=production NODE_OPTIONS='--dns-result-order=ipv4first' node railway-check.js && NODE_ENV=production NODE_OPTIONS='--dns-result-order=ipv4first' yarn start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100
  }
}